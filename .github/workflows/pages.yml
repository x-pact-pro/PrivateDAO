name: GitHub Pages Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment policy
        run: |
          echo "PrivateDAO live site is deployed from AWS only."
          echo "GitHub is storage/history, not a live publishing target."
